const {app} = require('../server/server');
const request = require('supertest');
const expect = require('expect');

describe('PUT /vehicles',()=> {
    it('should create a new vehicles collection',()=>{

    });
});
